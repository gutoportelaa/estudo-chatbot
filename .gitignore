# LangGraph
.langgraph_api/

# Imagens de referência/rascunho não entram no repo; versionamos apenas o que
# documenta o projeto (diagramas e capturas em docs/) e a marca.
*.png
!docs/**/*.png
!assets/ThinkAI.png

# Python / uv (backend)
api/.venv/
api/data/

.env
.coverage
test.db

.pytest_cache/
__pycache__/
*.pyc

# Node / bun (frontend)
web/node_modules/
web/dist/
web/*.tsbuildinfo
web/vite.config.js
web/vite.config.d.ts

*.pem
